/// TODO: Add more login up types that firebase supports
enum LoginType {
  emailAndPassword,
}// loginType

/// TODO: Add more sign up types that firebase supports
enum SignUpType {
  emailAndPassword
}// signUpType